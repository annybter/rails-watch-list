<h1>WatchList Creator</h1>

  <h2>Movie Lists</h2>
  <ol>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
</ol>
  <%= link_to 'Add list', new_list_path(@list), class: "btn btn-primary"%>
